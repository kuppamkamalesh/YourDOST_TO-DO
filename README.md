⚙ Part 3 – Backend Task
Title: Simple To-Do CRUD API

Objective:
Create simple REST APIs to manage to-do items.

Requirements:

APIs:

GET /todos → get all todos

POST /todos → create a todo

PUT /todos/:id → update a todo

DELETE /todos/:id → delete a todo

Store data either:

In memory, or

In a simple local JSON file (no database required)

Return proper JSON responses and status codes

Bonus:

Input validation

Add a field like completed: true/false

Deploy the backend (Render / Railway / Cyclic)

Deliverables:

Node.js (Express)

README with instructions

(Optional) Deployed API link
